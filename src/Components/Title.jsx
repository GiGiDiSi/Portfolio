import '../Styles/index.css';
const Titles = ({ title }) => {
  return(
    <h1 className="mb-[25px] text-9xl text-center font-bold uppercase tracking-wider">
        {title}    
    </h1>
  );
}

export default Titles